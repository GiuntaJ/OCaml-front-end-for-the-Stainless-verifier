let applied_types = [['a', 'b']; ['a', 'b']];;
let tuples_type = (true, 'a', ['a']);;
let functions_type_1 = fun x -> if x then 'a' else 'b';;
let functions_type_2 = fun x y -> if x && y = 10 then 'a' else 'b';;